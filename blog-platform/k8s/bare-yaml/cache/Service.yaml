apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  type: ClusterIP # only works inside the kubernetes cluster, you cant access via internet, only accessible to communication via ingress or gateway
  selector:
    type: cache
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
